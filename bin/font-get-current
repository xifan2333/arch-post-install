#!/bin/bash
# Get current system font
# Usage: font-get-current

FONT_FILE="$HOME/.config/font/current"

if [ -f "$FONT_FILE" ]; then
    cat "$FONT_FILE"
else
    # Return empty if no font is set
    echo ""
fi
