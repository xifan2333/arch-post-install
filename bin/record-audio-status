#!/bin/bash
# Show record_mix status
set -e
source record-audio-core
record_audio_status
if pgrep -f "python3 .*/record-audio-daemon$" >/dev/null 2>&1; then
    echo "daemon: running"
else
    echo "daemon: stopped"
fi
