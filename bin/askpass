#!/bin/bash
# Lightweight askpass using wofi (Wayland-native, won't break fcitx5).
# Usage: SUDO_ASKPASS=/path/to/this-script sudo -A <command>

exec wofi --dmenu --password --prompt="Password" --lines=1 2>/dev/null
