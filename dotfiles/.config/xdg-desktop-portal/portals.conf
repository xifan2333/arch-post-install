# XDG Desktop Portal backend configuration for River WM
# River is a wlroots-based compositor

[preferred]
# Use gtk for generic interfaces (FileChooser, etc.)
# Use wlr for wlroots-specific features (Screenshot, ScreenCast)
default=gtk;wlr


