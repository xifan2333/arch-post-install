[colors]
[colors.primary]
background = "{{ bg }}"
foreground = "{{ fg }}"
dim_foreground = "{{ fg_dim }}"

[colors.normal]
black = "{{ ansi0 }}"
red = "{{ ansi1 }}"
green = "{{ ansi2 }}"
yellow = "{{ ansi3 }}"
blue = "{{ ansi4 }}"
magenta = "{{ ansi5 }}"
cyan = "{{ ansi6 }}"
white = "{{ ansi7 }}"

[colors.bright]
black = "{{ ansi8 }}"
red = "{{ ansi9 }}"
green = "{{ ansi10 }}"
yellow = "{{ ansi11 }}"
blue = "{{ ansi12 }}"
magenta = "{{ ansi13 }}"
cyan = "{{ ansi14 }}"
white = "{{ ansi15 }}"

[colors.cursor]
text = "{{ bg }}"
cursor = "{{ cursor }}"

[colors.selection]
text = "{{ bg }}"
background = "{{ selection }}"
