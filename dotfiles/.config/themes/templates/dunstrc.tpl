# Auto-generated from colors.toml
[urgency_low]
    background = "{{ bg }}"
    foreground = "{{ fg }}"
    frame_color = "{{ fg_dim }}"

[urgency_normal]
    background = "{{ bg }}"
    foreground = "{{ fg }}"
    frame_color = "{{ yellow }}"

[urgency_critical]
    background = "{{ bg }}"
    foreground = "{{ red }}"
    frame_color = "{{ red }}"
