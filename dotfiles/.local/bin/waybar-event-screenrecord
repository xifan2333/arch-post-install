#!/bin/bash
# Waybar screen recording event handler
# Handles click events for screen recording control

source "$HOME/.local/lib/lib-screenrecord"

case "$1" in
    click)
        screenrecord_waybar_click
        ;;
    *)
        echo "Usage: $0 {click}"
        exit 1
        ;;
esac
