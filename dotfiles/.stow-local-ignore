# =========================================================
# Stow Ignore Patterns (Stow will NOT create links for these)
# =========================================================

# 1. Self-Reference & Version Control Metadata
# Stow must ignore its own ignore file, the Git repo, and its metadata.
\.stow-local-ignore
\.git
\.gitignore
# Optional: If you use .gitmodules for submodules
#\.gitmodules


# 2. Runtime / Dynamic Configuration Files
# Files that are modified by scripts or runtime processes.
# These files should be either managed by rsync -u (like bashrc), 
# or completely left alone by Stow.
\.config/current             # Used for dynamic theme/config switching
\.config/fcitx5/conf/cached_layouts # Fcitx5 runtime cache/layout data

# 3. Cache & Large Local Data Directories
# Directories containing large, frequently changing, or machine-specific data.
\.dotnet                     # .NET runtime cache files
\.cache/* # General XDG cache directory (if managed by Stow)
\.local/share/Trash          # Trash files

# 4. VSCode & IDE Runtime Data
# VSCode's volatile and workspace-specific data.
\.vscode/* # Local .vscode/ directory content
\.config/Code/User/History
\.config/Code/User/globalStorage
\.config/Code/User/workspaceStorage
\.config/Code/User/sync